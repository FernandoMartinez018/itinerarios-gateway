<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Decisión verificada (ver ADR-010-spring-cloud-version.md en itinerarios-docs):
        Spring Cloud 2025.1.1 ("Oakwood", liberado 2026-01-29) agregó compatibilidad
        explícita con Spring Boot 4.0.1+. Spring Cloud Gateway ya se documenta
        oficialmente sobre Spring Framework 7 / Spring Boot 4.
        Fuente: https://spring.io/blog/2026/01/29/spring-cloud-2025-1-1-aka-oakwood-has-been-released/
        Fuente: https://docs.spring.io/spring-cloud-gateway/reference/index.html

        IMPORTANTE: desde el release train 2025.0, el artefacto clásico
        "spring-cloud-gateway-server" quedó deprecado en favor de artefactos
        separados por stack (WebFlux / WebMVC). Verificar el nombre exacto del
        starter (spring-cloud-starter-gateway-server-webflux) contra el BOM real
        antes de construir, ya que la tabla de renombres no pudo confirmarse
        carácter por carácter desde este entorno sin acceso a Maven Central.
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>com.itinerarios</groupId>
    <artifactId>gateway</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>gateway</name>
    <description>API Gateway - Sistema de Itinerarios Personales</description>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2025.1.1</spring-cloud.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway-server-webflux</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
